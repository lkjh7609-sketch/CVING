# 자기소개서 작성 툴 - 상세 분석 및 작업계획서

## 1. 핵심 요구사항 분석

### 주요 기능 흐름
1. **기본 답변 작성** → 5개 문항에 대한 사용자의 기본 답변
2. **회사 정보 수집** → 최대 5개 회사의 공고 정보 (텍스트 + 스크린샷)
3. **AI 생성** → 각 회사의 실제 문항에 맞춰 기본 답변을 재구성
4. **결과 관리** → 탭 방식으로 확인하고 복사/저장

### 핵심 과제
- **OCR 처리**: 스크린샷에서 공고 내용 추출 + 수동 보정
- **유연한 AI 프롬프팅**: 회사별 문항 구조가 다를 수 있음을 고려
- **실시간 UI 반응성**: 문자수 체크, 로딩 상태, 탭 전환
- **데이터 영속성**: 작성 중인 내용 손실 방지

## 2. 기술 스택 제안

### 프론트엔드
**React 18 + Vite + TypeScript**
- **선택 이유**: 
  - Vite는 HMR이 빠르고 번들 크기가 작음
  - TypeScript로 타입 안정성 확보
  - React 18의 Concurrent Features로 로딩 상태 관리 용이

**상태 관리: Zustand**
- **선택 이유**:
  - Redux보다 훨씬 가볍고 보일러플레이트 적음
  - 로컬스토리지 미들웨어로 자동 저장 가능
  - 비동기 액션 처리 간단

**스타일링: Tailwind CSS + shadcn/ui**
- **선택 이유**:
  - 빠른 UI 구축 가능
  - 번들 크기 최적화 (사용한 클래스만 포함)
  - shadcn/ui로 고품질 컴포넌트 빠르게 구성

### OCR 처리
**Tesseract.js (클라이언트 사이드)**
- **선택 이유**:
  - 브라우저에서 직접 실행 (서버 불필요)
  - 한글 지원 양호
  - 무료, 오프라인 가능

**대안: OpenAI Vision API**
- OCR 정확도가 더 높지만 비용 발생
- 사용자 선택권 제공 (Tesseract 실패 시 Vision API 사용)

### 데이터 저장
**LocalStorage + IndexedDB**
- **선택 이유**:
  - 서버 불필요 (개인 툴이므로)
  - 작성 중인 내용 자동 저장
  - IndexedDB로 스크린샷 등 큰 데이터 저장

### AI API 연동
**AIApiFlow Gateway + Streaming**
- 스트리밍 응답으로 생성 과정 실시간 표시
- Abort Controller로 생성 취소 기능 구현

## 3. 상세 구현 계획

### 3.1 프로젝트 구조
```
cving/
├── src/
│   ├── components/
│   │   ├── QuestionInput.tsx          # 문항 입력 컴포넌트
│   │   ├── CharacterCounter.tsx       # 글자수 카운터
│   │   ├── CompanyCard.tsx            # 회사 정보 카드
│   │   ├── CompanyCardList.tsx        # 회사 카드 목록
│   │   ├── PromptModal.tsx            # 프롬프트 설정 모달
│   │   ├── ResultTabs.tsx             # 결과 탭
│   │   ├── OCRProcessor.tsx           # OCR 처리 인터페이스
│   │   └── ui/                        # shadcn/ui 컴포넌트
│   ├── stores/
│   │   ├── questionStore.ts           # 문항 상태 관리
│   │   ├── companyStore.ts            # 회사 정보 상태 관리
│   │   ├── promptStore.ts             # 프롬프트 상태 관리
│   │   └── resultStore.ts             # 생성 결과 상태 관리
│   ├── services/
│   │   ├── aiService.ts               # AI API 호출 로직
│   │   ├── ocrService.ts              # OCR 처리 로직
│   │   └── storageService.ts          # 로컬 저장 로직
│   ├── lib/
│   │   ├── types.ts                   # 타입 정의
│   │   └── utils.ts                   # 유틸 함수
│   ├── App.tsx
│   └── main.tsx
├── .env.local
├── package.json
├── vite.config.ts
└── tailwind.config.js
```

### 3.2 핵심 타입 정의
```typescript
interface Question {
  id: string;
  content: string;
  answer: string;
  maxLength: number;
  currentLength: number;
}

interface Company {
  id: string;
  name: string;
  jobPostingText: string;
  screenshot?: File | string; // File or base64
  ocrText?: string;
  ocrStatus: 'idle' | 'processing' | 'success' | 'failed';
}

interface Prompt {
  id: string;
  name: string;
  content: string;
  isDefault: boolean;
}

interface GenerationResult {
  companyId: string;
  status: 'idle' | 'generating' | 'success' | 'error';
  content: string;
  error?: string;
  streamedContent?: string;
}
```

### 3.3 AI 생성 로직 설계

**핵심 프롬프트 구조**:
```
시스템: 당신은 전문 자기소개서 작성 컨설턴트입니다.

컨텍스트:
- 지원자의 기본 답변 5개 (참고 자료)
- 목표 회사의 공고 내용
- 공고에서 추출한 실제 문항

작업:
1. 공고에서 자기소개서 문항 파악
2. 기본 답변을 분석하여 핵심 경험/역량 추출
3. 목표 회사의 문항에 맞춰 답변 재구성
4. 회사의 가치관/문화에 맞게 톤 조정

출력 형식:
[문항1]
답변...

[문항2]
답변...
```

**병렬 생성 vs 순차 생성**:
- 최대 5개 회사 동시 생성 시 API 부하 및 비용 고려
- **제안**: 2-3개씩 배치 처리 (Promise.all + 제한)
- 개별 재생성은 즉시 실행

### 3.4 OCR 처리 전략

**두 단계 접근**:
1. **1차**: Tesseract.js로 무료 OCR 시도
   - 한글 언어팩 로드
   - 이미지 전처리 (대비 조정, 노이즈 제거)
   - 신뢰도 점수 확인

2. **2차** (옵션): 신뢰도 낮을 시 OpenAI Vision API 제안
   - 사용자 선택 시에만 호출
   - 비용 표시

3. **수동 보정**: 
   - OCR 결과를 편집 가능한 텍스트 영역에 표시
   - 사용자가 직접 수정 가능

### 3.5 성능 최적화

**번들 크기 최적화**:
- Tesseract.js는 lazy load (실제 사용 시에만)
- Code splitting: 각 기능별 청크 분리
- Tree shaking: 미사용 코드 제거

**렌더링 최적화**:
- React.memo로 불필요한 리렌더링 방지
- useMemo/useCallback 활용
- 가상 스크롤 (회사 카드가 많을 경우)

**API 호출 최적화**:
- AbortController로 중복 요청 취소
- 디바운싱 (자동저장 시)
- 스트리밍 응답으로 체감 속도 향상

## 4. UI/UX 설계

### 레이아웃 구조
```
┌────────────────────────────────────────┐
│  Header: 자기소개서 작성 도우미         │
├────────────────────────────────────────┤
│  Section 1: 기본 문항 입력              │
│  ┌──────────────────────────────────┐  │
│  │ 문항1 [1000자] 현재: 245/1000    │  │
│  │ [텍스트 영역]                    │  │
│  └──────────────────────────────────┘  │
│  ... (문항 5개)                         │
├────────────────────────────────────────┤
│  Section 2: 지원 회사 정보              │
│  [+ 회사 추가]                          │
│  ┌─────────┐ ┌─────────┐               │
│  │ 회사A   │ │ 회사B   │               │
│  │ [수정]  │ │ [수정]  │               │
│  └─────────┘ └─────────┘               │
├────────────────────────────────────────┤
│  [프롬프트 설정] [전체 생성] [재생성]   │
├────────────────────────────────────────┤
│  Section 3: 생성 결과                   │
│  [회사A] [회사B] [회사C]                │
│  ┌──────────────────────────────────┐  │
│  │ 생성된 자기소개서 내용            │  │
│  │ [복사] [다운로드]                 │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘
```

### 주요 인터랙션

1. **문항 입력**:
   - 실시간 글자수 카운팅
   - 초과 시 빨간색 경고 + 입력 제한 옵션

2. **회사 카드**:
   - 드래그 앤 드롭으로 스크린샷 업로드
   - 카드 호버 시 편집/삭제 버튼 표시
   - 펼침/접힘 토글

3. **OCR 처리**:
   - 업로드 즉시 자동 OCR 시작
   - 진행 상태 표시 (스피너 + 퍼센트)
   - 결과 편집 가능

4. **생성 과정**:
   - 버튼 클릭 → 각 회사 탭에 로딩 애니메이션
   - 스트리밍 시 텍스트 점진적으로 표시
   - 완료 시 성공 알림

5. **결과 관리**:
   - 탭 클릭으로 회사별 결과 전환
   - 개별 복사/다운로드
   - 일괄 다운로드 (ZIP)

## 5. 보완 및 제안 사항

### 5.1 기능 추가 제안

**1. 템플릿 관리**
- 자주 쓰는 기본 답변을 템플릿으로 저장
- 여러 버전 관리 (경력직용, 신입용 등)

**2. 히스토리 관리**
- 과거 생성 결과 저장
- 날짜별/회사별 조회

**3. 분석 기능**
- 생성된 자소서의 키워드 빈도 분석
- 공고와의 매칭도 점수 표시

**4. 공고 스크래핑**
- URL 입력 시 자동으로 공고 내용 추출
- 주요 채용 사이트 지원 (사람인, 잡코리아 등)

**5. 비교 모드**
- 여러 생성 결과를 나란히 비교
- 차이점 하이라이트

### 5.2 개선 제안

**1. AI 모델 선택**
- GPT-4, Claude 등 모델 선택 옵션
- 가격/품질 트레이드오프 표시

**2. 프롬프트 마켓플레이스**
- 커뮤니티 프롬프트 공유
- 평점/리뷰 시스템

**3. 협업 기능 (선택적)**
- 멘토에게 피드백 요청
- 익명 링크 공유

**4. 모바일 지원**
- 반응형 디자인
- PWA로 앱처럼 사용

**5. 접근성**
- 키보드 네비게이션
- 스크린 리더 지원

### 5.3 보안 고려사항

**1. API 키 보호**
- .env 파일 절대 커밋 금지
- 프론트엔드에서 직접 호출 (개인 툴이므로 허용)
- 프로덕션 시 백엔드 프록시 권장

**2. 데이터 프라이버시**
- 모든 데이터는 로컬 저장
- 외부 전송은 AI API 호출뿐
- 사용자에게 명확히 고지

**3. 입력 검증**
- XSS 방지 (DOMPurify)
- 파일 타입 체크 (이미지만)
- 파일 크기 제한 (10MB)

## 6. 구현 단계별 계획

### Phase 1: 기본 골격 (1-2일)
- [x] 프로젝트 초기화 (Vite + React + TS)
- [ ] Tailwind + shadcn/ui 설정
- [ ] 기본 레이아웃 구성
- [ ] 라우팅 없는 단일 페이지 구조

### Phase 2: 문항 입력 기능 (1일)
- [ ] QuestionInput 컴포넌트
- [ ] 글자수 카운터
- [ ] Zustand 스토어 연결
- [ ] LocalStorage 자동 저장

### Phase 3: 회사 정보 관리 (2-3일)
- [ ] CompanyCard 컴포넌트
- [ ] CRUD 기능 구현
- [ ] 드래그 앤 드롭 업로드
- [ ] IndexedDB 이미지 저장

### Phase 4: OCR 통합 (2일)
- [ ] Tesseract.js 통합
- [ ] 이미지 전처리
- [ ] OCR 상태 표시
- [ ] 수동 편집 UI

### Phase 5: AI 연동 (2-3일)
- [ ] AIApiFlow API 클라이언트
- [ ] 스트리밍 응답 처리
- [ ] 에러 핸들링
- [ ] 재생성 로직

### Phase 6: 프롬프트 관리 (1일)
- [ ] 프롬프트 모달
- [ ] 프리셋 저장/불러오기
- [ ] 기본 프롬프트 템플릿

### Phase 7: 결과 표시 (1-2일)
- [ ] 탭 컴포넌트
- [ ] 로딩 상태
- [ ] 복사/다운로드 기능
- [ ] 마크다운 렌더링 (선택)

### Phase 8: 최적화 및 테스트 (2일)
- [ ] 성능 프로파일링
- [ ] 번들 크기 최적화
- [ ] 크로스 브라우저 테스트
- [ ] 에러 바운더리

### Phase 9: 추가 기능 (선택, 1-2일)
- [ ] 템플릿 관리
- [ ] 히스토리
- [ ] 분석 기능

**총 예상 기간**: 10-15일 (핵심 기능 기준)

## 7. 기술적 챌린지 및 해결 방안

### Challenge 1: OCR 정확도
**문제**: 한글 공고 이미지의 OCR 정확도 낮을 수 있음
**해결**:
- 이미지 전처리 (Contrast, Brightness 조정)
- 다국어 언어팩 (kor+eng)
- Vision API 폴백 옵션
- 사용자 수동 보정 강조

### Challenge 2: AI 응답 품질
**문제**: 회사별 문항 구조 파악 실패 가능
**해결**:
- 프롬프트에 Few-shot 예시 포함
- 구조화된 출력 형식 강제 (JSON mode)
- 사용자가 문항 구조 힌트 제공 옵션

### Challenge 3: 대용량 데이터 관리
**문제**: 스크린샷 5개 * 여러 버전 = 로컬스토리지 한계
**해결**:
- IndexedDB 사용 (50MB+)
- 이미지 압축 (Client-side)
- 오래된 데이터 정리 알림

### Challenge 4: 비용 관리
**문제**: AI API 호출 비용
**해결**:
- 토큰 사용량 표시
- 프롬프트 최적화 (불필요한 컨텍스트 제거)
- 캐싱 (같은 회사 재생성 시 공고 분석 재사용)

## 8. 최종 권장 사항

### 즉시 구현 (MVP)
1. ✅ 5개 문항 입력 + 글자수 체크
2. ✅ 회사 정보 입력 (텍스트 위주)
3. ✅ 기본 AI 생성
4. ✅ 결과 복사 기능

### 우선순위 높음
5. ✅ OCR 기능
6. ✅ 프롬프트 프리셋
7. ✅ 로컬 저장
8. ✅ 개별 재생성

### 우선순위 중간
9. 템플릿 관리
10. 히스토리
11. 일괄 다운로드

### 우선순위 낮음 (향후)
12. 분석 기능
13. 공고 스크래핑
14. 협업 기능

---

## 시작할 준비가 되었습니다!

제가 제안한 계획에 동의하시면 바로 구현을 시작하겠습니다. 수정하고 싶은 부분이나 우선순위 조정이 필요하면 말씀해주세요.

**첫 단계로 진행할 내용**:
1. Vite + React + TypeScript 프로젝트 초기화
2. Tailwind CSS + shadcn/ui 설정
3. 기본 폴더 구조 생성
4. 환경 변수 설정

바로 시작할까요?
